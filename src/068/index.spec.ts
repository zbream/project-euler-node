import { example068, main068 } from ".";

test('068', () => {
  const example = example068();
  expect(example).toBe('432621513')
  const result = main068();
  expect(result).toBe('6531031914842725');
});
